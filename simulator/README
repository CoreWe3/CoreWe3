#Assemble
./assembler [Binary File] < [Input Assembly]

#Disassemble
./disassembler [Binary File] > [Output File]

#Simulate
./simulator [Source Binary File] (-i [Input Binary File]) (-o [Output Binary File]) (-l [Insutruction Limit]) (-b [Break Point]) (-r [RAM Dump Data]) (-s [RAM Initial Data])

#View Binary File
./hex.sh [Binary File]

# コンパイルにはclangが必要です。aptからインストールすると
# ライブラリのパスがうまく通らないようなので、
# 環境変数の設定が必要かもしれません。

# $ sudo apt-get update
# $ sudo apt-get install clang
# $ export LD_LIBRARY_PATH=/usr/lib/x86_64-linux-gnu/:`echo $LD_LIBRARY_PATH`

